import { SelectionListValidatorDirective } from './selection-list-validator.directive';

describe('SelectionListValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectionListValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
